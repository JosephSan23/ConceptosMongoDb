use("platzi_store")

db.products.updateOne(
    // query
    { _id: 2 },
    // change => operators
    {
        //operador para modificar, se basa en modificar un valor entero
        $inc: {
            price: 100,
        }
    }
);

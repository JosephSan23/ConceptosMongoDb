use("platzi_store")

db.products.updateOne(
    // query
    { _id: 2 },
    // change => operators
    {
        //operador para modificar
        $set: {
            name: 'Change name',
            price: 3000,
            tags: ['A', 'B', 'C']
        }
    }
);
